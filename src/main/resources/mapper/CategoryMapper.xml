<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.accountbook.mapper.CategoryMapper">

    <resultMap id="categoryMap" type="com.example.accountbook.entity.Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, type, description,created_at)
        VALUES (#{name}, #{type}, #{description},NOW())
    </insert>

    <update id="update">
        UPDATE category
        SET name        = #{name},
        type        = #{type},
        description = #{description}
        updated_at    = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="categoryMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="categoryMap">
        SELECT * FROM category ORDER BY type, id
    </select>

    <select id="findByType" resultMap="categoryMap">
        SELECT * FROM category WHERE type = #{type} ORDER BY id
    </select>

</mapper>